<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="buddiesViewModel"
            type="pl.pollub.android.shoppinglist.viewmodel.BuddiesViewModel"/>
        <variable name="view"
            type="pl.pollub.android.shoppinglist.activity.BuddiesActivity"/>
    </data>

    <android.support.v4.widget.DrawerLayout
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <include
                android:id="@+id/toolbar_layout"
                layout="@layout/toolbar" />

            <android.support.v7.widget.RecyclerView
                android:id="@+id/buddies_recycler_view"
                android:scrollbars="vertical"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                app:items="@{buddiesViewModel.getModel}"
                app:itemViewBinder="@{buddiesViewModel.itemViewBinder}"
                app:clickHandler="@{view.clickHandler}"
                app:longClickHandler="@{view.longClickHandler}" />

            <android.support.design.widget.FloatingActionButton
                android:id="@+id/floatingActionButton"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_gravity="right"
                android:layout_margin="10dp"
                android:clickable="true"
                app:backgroundTint="@color/colorPrimary"
                app:fabSize="mini"
                app:srcCompat="@android:drawable/ic_menu_add" />
                <!--app:onClickListener="@{view.onButtonClick}" />-->

        </LinearLayout>

        <include
            android:id="@+id/burger_layout"
            layout="@layout/burger_menu" />

    </android.support.v4.widget.DrawerLayout>
</layout>
