<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.pollub.android.shoppinglist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainScreen" />
        <activity android:name=".LogInto" />
        <activity android:name=".Register" />
        <activity android:name=".ListsList" />
        <activity android:name=".ListItems" />
        <activity android:name=".FriendsList" />
        <activity android:name=".AddFriend" />
        <activity android:name=".Add_Product" />
        <activity android:name=".Settings" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Templates" />
        <activity android:name=".TemplatesList"></activity>
    </application>

</manifest>